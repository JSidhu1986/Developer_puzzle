import { InjectionToken } from '@angular/core';

export const StocksAppConfigToken = new InjectionToken('StocksAppConfig');
export const PRICEQUERY_ERROR_MESSAGE = 'No data available for the selected option due to: ';
